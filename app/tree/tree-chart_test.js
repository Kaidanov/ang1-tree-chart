'use strict';

describe('myApp.tree module', function() {

  beforeEach(module('myApp.tree'));

  describe('tree-ctrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TreeCtrl = $controller('TreeCtrl');
      expect(TreeCtrl).toBeDefined();
    }));

  });
});
